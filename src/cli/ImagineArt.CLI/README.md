# ImagineArt.CLI

Command-line interface for the ImagineArt SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global ImagineArt.CLI --prerelease
```

## Usage

```bash
imagine-art --help
imagine-art api --help
```